
import { useState, useEffect, useRef } from 'react';

// "undefined" means the URL will be computed from the `window.location` object

export default function BlocAnniversaire() {
    return <h1>Anniversaires</h1>
}